# Derived Profile Fields

> Status: Placeholder

The canonical MVP inventory and LLM-access rules for derived fields are defined in `user-profile-fields.md`. This document will add implementation-level types, derivation formulas, scoring versions, freshness, storage, and visibility rules without redefining that inventory.

TODO: Specify those implementation details for baseline scores, current-balance loads, confidence, freshness, top current signals, exclusion flags, recommendation history, content interests, and the allowlisted LLM profile summary.
